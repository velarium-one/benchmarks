use super::ReleasePin;

// Populate only after the release URL and exact archive/engine identities are approved.
// An empty table means unpublished, not an unverified download or a latest-release lookup.
pub const PINS: &[ReleasePin<'static>] = &[];
