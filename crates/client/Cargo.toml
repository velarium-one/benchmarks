[package]
name = "client"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
config = { path = "../config" }
libloading = "0.8"
thiserror = "1"

[build-dependencies]
config = { path = "../config" }
