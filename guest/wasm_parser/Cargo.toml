[package]
name = "riscv-fixture-wasm-parser"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
wasmparser = { version = "=0.244.0", default-features = false }
# Preserve the existing workload's dependency closure in all three workspaces.
bitflags = { version = "=2.13.1", default-features = false }

guest-kit = { path = "../common/support" }
