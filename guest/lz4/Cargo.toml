[package]
name = "riscv-fixture-lz4"
version = "0.1.0"
edition = "2024"

[dependencies]
lz4_flex = { version = "=0.14.0", default-features = false, features = ["checked-decode", "safe-decode"] }

[target.'cfg(target_arch = "riscv32")'.dependencies]
riscv-fixture-allocator = { path = "../allocator" }
riscv-fixture-kernel = { path = "../kernel" }
